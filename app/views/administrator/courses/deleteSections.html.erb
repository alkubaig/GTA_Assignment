   <%= link_to "&larr; Back".html_safe, editAll_administrator_courses_path %>

  <%= render 'styling' %>

  <h1>Sections</h1>
  
  
  <%= form_tag(deleteSelectedSections_administrator_courses_path , method: "delete") do %> 	
   <div style="display: inline-block; width: 80%;">
<table class="table " >

	<thead>
     <%= render 'sections_table' %>
  </thead>
  <tbody>
  
  <% @sections.each do |section| 
   @section = section
   %>

	<tr>	
	  <td><input name="course[section_id][]" type="checkbox" value= <%= section.id%> /> </td>
     <%= render 'section_info' %>
     </tr>
     
     <%end%>

  </tbody>
  
</table>

  </div>

<%= submit_tag "Delete Selected Sectionss", class: "btn btn-danger pull-right", data: {confirm: "Are you sure?"}  %>
<%end%>



 <br>

 <br>

 <br>





